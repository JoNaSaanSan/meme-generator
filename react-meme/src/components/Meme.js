// Meme Class

class Meme {
    constructor(url, id, width, height, name, boxcount) {
      this.url = url;
      this.id = id;
      this.width = width;
      this.height = height;
      this.name = name;
      this.boxcount = boxcount;
    }
  }

  export default Meme